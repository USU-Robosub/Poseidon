cmake_minimum_required(VERSION 3.3)
project(Drivers)

set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -std=c++11")

include_directories(include)
file(GLOB_RECURSE SOURCE_FILES "src/*.cpp")

add_library(Drviers SHARED ${SOURCE_FILES})